name: 🧩 CD · Native

on:
  workflow_call:
    inputs:
      ref:
        description: Build commit.
        required: true
        type: string
      version:
        description: Resolved version.
        required: true
        type: string

permissions: {}

jobs:
  native:
    name: native / ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 120
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-amd64
            runner: ubuntu-latest
            distribution: temurin
            kind: linux
            platform: linux/amd64
            extension: .native
          - name: linux-arm64
            runner: ubuntu-24.04-arm
            distribution: temurin
            kind: linux
            platform: linux/arm64
            extension: .native
          - name: macos-x64
            runner: macos-15-intel
            distribution: graalvm
            kind: host
            platform: ''
            extension: .native
          - name: macos-arm64
            runner: macos-latest
            distribution: graalvm
            kind: host
            platform: ''
            extension: .native
          - name: windows-x64
            runner: windows-latest
            distribution: graalvm
            kind: host
            platform: ''
            extension: .exe
    steps:
      - name: "🧑‍💻 Checkout [${{ inputs.ref }}]"
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 1
          ref: ${{ inputs.ref }}
          submodules: recursive

      - name: "🔍 Read Java Info"
        id: java_info
        uses: YunaBraska/java-info-action@11a434ffbf6bb3357363d1933be71a4076a90a6b # v3

      - name: "☕ Setup Java [${{ matrix.kind == 'host' && '25' || steps.java_info.outputs.java_version }}]"
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5
        with:
          distribution: ${{ matrix.distribution }}
          java-version: ${{ matrix.kind == 'host' && '25' || steps.java_info.outputs.java_version }}
          cache: maven

      - name: "🏷️ Set Project Version [${{ inputs.version }}]"
        shell: sh
        run: |
          set -eu
          chmod +x ./mvnw
          build_output_timestamp=$(git log -1 --format=%cI HEAD)
          ./mvnw -B versions:set -DnewVersion="${{ inputs.version }}" -DgenerateBackupPoms=false
          ./mvnw -B versions:set-property -Dproperty=project.build.outputTimestamp -DnewVersion="${build_output_timestamp}" -DgenerateBackupPoms=false
          printf 'build_output_timestamp=%s\n' "${build_output_timestamp}"
          printf 'version=%s\n' "${{ inputs.version }}"

      - name: "🐳 Setup Buildx"
        if: matrix.kind == 'linux'
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: "🧩 Build Native [${{ matrix.name }}]"
        if: matrix.kind == 'linux'
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: Dockerfile_Native
          target: export
          outputs: type=local,dest=target
          platforms: ${{ matrix.platform }}

      - name: "🧩 Build Native [${{ matrix.name }}]"
        if: matrix.kind == 'host' && matrix.name != 'windows-x64'
        shell: sh
        run: |
          set -eu
          chmod +x ./mvnw
          ./mvnw -B -Pnative -DskipTests package

      - name: "🧩 Build Native [${{ matrix.name }}]"
        if: matrix.name == 'windows-x64'
        shell: cmd
        run: |
          for /f "usebackq delims=" %%i in (`"%ProgramFiles(x86)%\Microsoft Visual Studio\Installer\vswhere.exe" -latest -products * -requires Microsoft.VisualStudio.Component.VC.Tools.x86.x64 -property installationPath`) do set "vs_path=%%i"
          if not defined vs_path (
            echo MSVC installation not found. 1>&2
            exit /b 1
          )
          call "%vs_path%\Common7\Tools\VsDevCmd.bat" -arch=x64 -host_arch=x64
          call mvnw.cmd -B -Pnative -DskipTests package

      - name: "📦 Prepare Native Asset [${{ matrix.name }}]"
        shell: sh
        run: |
          set -eu
          artifact_id=$(awk -F '[<>]' '/^[[:space:]]*<artifactId>/ { print $3; exit }' pom.xml)
          test -n "${artifact_id}"
          case "${{ matrix.kind }}" in
            linux)
              source=target/app.native
              ;;
            host)
              source="target/${artifact_id}.native"
              if [ "${{ matrix.extension }}" = '.exe' ]; then
                source="${source}.exe"
              fi
              ;;
          esac
          test -f "${source}"
          asset="${artifact_id}-${{ matrix.name }}-${{ inputs.version }}${{ matrix.extension }}"
          mkdir -p release-assets
          cp "${source}" "release-assets/${asset}"
          chmod +x "release-assets/${asset}"
          printf 'asset=%s\n' "${asset}"
          printf 'source=%s\n' "${source}"

      - name: "📤 Upload Release Asset [${{ matrix.name }}]"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: release-assets-${{ matrix.name }}
          path: release-assets/*
          if-no-files-found: error
          retention-days: 1
