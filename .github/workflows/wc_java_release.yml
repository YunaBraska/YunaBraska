name: 🏷️ CD · Release

on:
  workflow_call:
    outputs:
      commit_sha:
        description: Build commit.
        value: ${{ jobs.build.outputs.commit_sha }}
      dry_run:
        description: Publish a snapshot instead of a release.
        value: ${{ jobs.build.outputs.dry_run }}
      version:
        description: Resolved release version.
        value: ${{ jobs.build.outputs.version }}
    inputs:
      force:
        description: Publish unchanged source.
        required: false
        default: false
        type: boolean
      dry_run:
        description: Publish a snapshot.
        required: false
        default: false
        type: boolean
      semver_strategy:
        description: Semver next strategy.
        required: false
        default: ''
        type: string
      upstream_version:
        description: Upstream version.
        required: false
        default: ''
        type: string
permissions: {}

concurrency:
  group: java-release-${{ github.repository }}
  cancel-in-progress: false

jobs:
  mode:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      dry_run: ${{ steps.release_info.outputs.dry_run }}
    steps:
      - name: "🧑‍💻 Checkout"
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7
        with:
          fetch-depth: 0

      - name: "🏷️ Release Mode"
        id: release_info
        shell: sh
        run: |
          set -eu
          case "${{ inputs.semver_strategy }}" in
            snapshot)
              dry_run=false
              ;;
            '')
              dry_run=true
              if [ "${{ inputs.force }}" = "true" ]; then
                dry_run=false
              elif [ -n "${{ inputs.upstream_version }}" ]; then
                if ! git rev-parse -q --verify "refs/tags/${{ inputs.upstream_version }}" >/dev/null; then
                  dry_run=false
                fi
              else
                latest_tag=$(git tag --merged HEAD --sort=-creatordate | sed -n '1p')
                if [ -z "${latest_tag}" ] || ! git diff --quiet "${latest_tag}"..HEAD -- pom.xml src; then
                  dry_run=false
                fi
              fi
              ;;
            *)
              dry_run=false
              ;;
          esac

          if [ "${{ inputs.dry_run }}" = "true" ] || [ "${GITHUB_REF}" != "refs/heads/${{ github.event.repository.default_branch }}" ]; then
            dry_run=true
          fi

          {
            printf 'dry_run=%s\n' "${dry_run}"
          } >> "${GITHUB_OUTPUT}"
          printf 'dry_run=%s\n' "${dry_run}"
          printf 'semver_strategy=%s\n' "${{ inputs.semver_strategy }}"
          printf 'upstream_version=%s\n' "${{ inputs.upstream_version }}"

  build:
    needs:
      - mode
    permissions:
      contents: read
    uses: ./.github/workflows/wc_java_build_common.yml
    with:
      ref: ${{ github.sha }}
      semver_strategy: ${{ inputs.semver_strategy }}
      upstream_version: ${{ inputs.upstream_version }}
      dry_run: ${{ needs.mode.outputs.dry_run == 'true' }}
      upload_artifact: true
